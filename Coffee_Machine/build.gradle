plugins {
    id 'application'
}

repositories {
    mavenCentral()
}

dependencies {

}

application {
    mainClass = 'coffeemachine.CoffeeMachine'
}

run {
    // Wait for input when using System.in.
    standardInput = System.in
}